import "./Header.scss";

import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="component-header">
      <h1>Product Grid</h1>
    </header>
  );
};

export default Header;